{
  "name": "otp-router-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Multi-client OTP routing server. Matches an OTP coming from a phone to the exact browser waiting for that phone number, and pushes it for auto-fill.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
